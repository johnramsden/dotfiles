# Platform-specific ignores
{{ if .isCan }}
# Skip flatpak timer on Ubuntu (managed differently or not needed)
.config/systemd/user/update-flatpaks.service
.config/systemd/user/update-flatpaks.timer
{{ else }}
# Skip snap config on Arch
.config/environment.d/50-snap.conf
.Xresources
{{ end }}

# Don't deploy these to home
README.md
bootstrap.sh
